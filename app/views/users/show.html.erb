<h1>Welcome <% @current_user.name %>! </h1>

<h2>Your beer list!</h1>
    <% @current_user.review.each do |r| %>
        <%= link_to beer_name(r.beer), beer_rating_path(r.beer, r.id) %> 
    <% end %>
<br>
<br>
<p><%= link_to 'Add a new beer', new_beer_path %></p>